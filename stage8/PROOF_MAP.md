# Proof Map

| Result | Proof strategy | Critical sign / condition |
|---|---|---|
| Private effort decline | Implicit Function Theorem | `ω_S'<0`, `Φ_{P,e}<0` |
| Coordinated effort increase | Implicit Function Theorem | condition (G), `Φ_{C,e}<0` |
| Main P3 divergence | Combine the two IFT signs | private decline + condition (G) |
| Cross-route necessity | Set `D_R'=0` | coordinated numerator becomes negative |
| One-for-one interpretation | Substitute `ω_R'=-ω_S'`, `D_R'=D_S'` | reduces to `A_R>A_S` |
| Benchmark effort levels | Closed-form FOCs | `k>0`, `k>β²` for social problem |
| `e^P<e^C<e^W` | Direct algebra | `0<m<p-c_S`, `a>p`, `c_S>c_R`, `k>β²` |
| Widening `e^C-e^P` | Differentiate closed forms | `h'>0`, `c_S>c_R`, `m>0` |
| Widening `e^W-e^P` | Differentiate closed forms | same plus `k>β²` |
| Local under-retention | Private indifference + positive `π_S` + CS gain + continuity | existence of a genuine private retention threshold |

## Completeness audit

- FOCs: specified.
- SOC/concavity signs: explicitly required.
- IFT denominator signs: stated.
- No division by zero under frozen restrictions.
- Interior benchmark region: nonempty.
- Demand: positive because `a>p` and `e≥0`.
- Benchmark efforts: positive for `0<h<1`.
- Social welfare: strictly concave in effort because `k>β²`.
- Under-retention: frozen only as a local conditional result; no global strengthening.
- General theorem and benchmark use the same appropriability / cross-route information mechanism.

**Proof completeness status: PASS for freeze purposes.**
