# Notation Map

## Stage 7 → Stage 8 canonical notation

| Stage 7 | Stage 8 | Reason |
|---|---|---|
| `s(η)` | `ω_S(η)` | Avoid collision with the old seminar-paper cross-price parameter `s` |
| `r(η)` | `ω_R(η)` | Symmetric route-weight notation |

No economic content changes.

## Old seminar-paper symbols not reused as canonical theory primitives

- old `s`: cross-price coefficient — discarded;
- `θ`: post-equilibrium profit allocation — discarded;
- `w_2`: canceled from the old joint-profit problem — discarded.

Stage 8 notation must not silently revive these old interpretations.
