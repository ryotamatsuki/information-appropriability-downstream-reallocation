# Notation Ledger

| Symbol | Canonical meaning |
|---|---|
| `M` | upstream producer |
| `S` | information-producing downstream node |
| `R` | alternative downstream route |
| `η` | exogenous downstream-reallocation / route-prominence index |
| `e` | downstream information-production effort |
| `x` | upstream product improvement generated by information |
| `ω_S(η)` | activity weight of `S`, decreasing in `η` |
| `ω_R(η)` | activity weight of `R`, increasing in `η` |
| `D_S(x), D_R(x)` | product-response / demand functions |
| `C(e)` | effort cost |
| `m_S` | private own-route return captured by `S` per activity unit in the general model |
| `A_S, A_R` | coordinated per-unit system returns through `S` and `R` |
| `e^P` | private information effort |
| `e^C` | vertically coordinated information effort |
| `e^W` | social-planner information effort |
| `h(η)` | mass assigned to the alternative route in the microfounded benchmark |
| `a,p` | benchmark demand intercept / non-strategic price parameters |
| `c_S,c_R` | real distribution costs in the benchmark |
| `m,b,δ` | benchmark private-margin mapping inherited from the tractable Stage-4 block |
| `β,k` | benchmark linear learning productivity and quadratic cost parameter |

Notation status: **FROZEN**, subject only to the theory-change policy.
